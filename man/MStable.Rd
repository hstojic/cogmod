% Generated by roxygen2 (4.1.0): do not edit by hand
% Please edit documentation in R/ModelSelection.R
\name{MStable}
\alias{MStable}
\title{All the information for model selection in one data frame.}
\usage{
MStable(model_names, logLik, noPar, noObs, latex = FALSE)
}
\arguments{
\item{model_names}{A character vector with names of the models.}

\item{logLik}{A numeric vector with log likelihoods for each model.}

\item{noPar}{A numeric vector with number of parameters for each model.}

\item{noObs}{A numeric vector with number of observations for each model.}

\item{latex}{A logical argument, TRUE if latex formatted table is needed, FALSE by default.}
}
\value{
A data frame with the information on the models that is recommended for the model selection.
}
\description{
Information important for model selection in one place - number of parameters, log likelihood, AIC, BIC, AIC and BIC delta, AIC and BIC weights.
}
\examples{
# example from Wagenmakers and Farrel (2004) article
logLik <- c(-100, -98, -100, -99, -103)
model_names <- c("A1", "A2", "B1", "B2", "C")
noPar <- c(2, 3, 3, 4, 4)
noObs <- rep(240, 5)
MStable(model_names, logLik, noPar, noObs)
}

